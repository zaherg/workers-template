{
	"name": "uuid",
	"version": "0.0.1",
	"private": true,
	"prettier": "@zaher/prettier-config",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"env": "dotenv -e ./.dev.vars --",
		"start": "wrangler dev",
		"style:fix": "prettier . --write",
		"test": "vitest",
		"typegen": "wrangler types",
		"postinstall": "touch .dev.vars"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.1.0",
		"@cloudflare/workers-types": "^4.20240512.0",
		"@zaher/prettier-config": "^1.0.4",
		"dotenv-cli": "^7.4.2",
		"prettier": "^3.2.5",
		"typescript": "^5.0.4",
		"vitest": "1.3.0",
		"wrangler": "^3.0.0"
	},
	"dependencies": {
		"hono": "^4.3.7"
	}
}
